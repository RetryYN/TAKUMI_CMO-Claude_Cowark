---
description: 動画コンテンツ台本生成 — 商材/記事/キービジュアルから、プラットフォーム別のオーガニック動画（YouTube/Reels/TikTok/ショート）の構成・台本・絵コンテを生成する。Use when ユーザーが「動画の構成を作って」「YouTube/Reels/TikTokの台本」「この記事を動画にして」「ショート動画のフック案」等、オーガニック動画コンテンツの企画・台本を依頼したとき。Not for 静止画、動画ファイル自体の生成（対象外と伝える）。有料の動画広告は扱わない（ゼロ広告費）。
argument-hint: <記事/LPのURL または 商材の説明> [投稿先: tiktok/youtube/reels（省略時は3種全部）]
---

オーガニック動画コンテンツの台本・絵コンテを作成してください。入力: $ARGUMENTS

## 手順

1. **素材の把握**: 入力が URL なら knowledge/styles/・該当ブランドの既存データを優先再利用（なければ read_page で訴求・実績数値・CTA文言を抽出）。キービジュアルが添付されていればそのトーン・コピーも入力に加える
2. **skills/video-script/SKILL.md を Read** し、その動画構成規範に従う（オーガニック動画に翻案）:
   - 最強の1訴求を選定（1動画1メッセージ）→ フック型を選びフック代案3本
   - 黄金構造（0-3秒フック→価値→実証→CTA）で絵コンテ表を作成
   - 指定された投稿先の型（尺・比率・テロップ量）に調整。指定がなければ TikTok系/YouTube/Reels の3バージョン
3. **文言の品質**: コピーは copywriting スキル（13字ルール）、実証は実数値のみ（捏造禁止・ないものは `[要確認]`）、CTA はブランドの導線と同一文言
4. **検収**: SKILL.md 末尾のチェックリストで自己検査。lessons.md（knowledge/feedback/）があれば反映。**最終稿は skills/ad-compliance-jp/SKILL.md の表現規制チェック（景表法・薬機法・ステマ表記等）を必ず通してから納品**
5. **出力**: `knowledge/drafts/videoscript-<name>-<date>.md` に保存し、チャットにも絵コンテ表を表示。複数プラットフォーム版は差分（尺・フック・テロップ）を明示

## 注意

- 動画ファイルの生成・編集は対象外（台本・絵コンテ・フック設計まで）。求められたら制作ツールへの受け渡し形式（この絵コンテ表）がそのまま使えると案内。動画生成は docs/parts/videogen.md、加工は docs/media-pipeline.md
- 他者事例の映像表現を「参考」として言及するのは可、コピーの転用は不可
